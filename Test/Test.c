#include <stdio.h>
#include <string.h>

int najdi_median (int *pole, int n) {
    int median;

    return median;
}

void f(int x){
    if(x > 1)
    {
        f(x/2);
    }
    printf("%i", x%2);
}

int main(){

char *retaz = "ABC";
printf("%s\n", retaz);
printf("%s\n", retaz+1);
printf("%c\n", retaz[1]);
printf("%lu\n", strlen(retaz));
}

